fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
httpx>=0.27.0
twilio>=9.0.0
pydantic>=2.7.0
pydantic-settings>=2.4.0
structlog>=24.0.0
asyncpg>=0.29.0
xai-sdk>=1.10.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
jinja2>=3.1.4
bcrypt>=4.0.0
# audioop was removed from the stdlib in Python 3.13 (PEP 594). audioop-lts
# is a drop-in backport (`from audioop import ulaw2lin` keeps working). Used
# in recording.py for μ-law -> 16-bit PCM decoding.
audioop-lts>=0.2.1 ; python_version >= "3.13"
tzdata>=2025.1   ; sys_platform == "win32"
